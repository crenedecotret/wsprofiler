# Plan: Rename session file extension from `.wsprof` to `.wsp2`

## Objective
Change the wizard session save file extension from `.wsprof` to `.wsp2` everywhere it is exposed to the user (save dialogs, open dialogs, defaults) and update all related references in code, tests, and documentation.

## Rationale
The user wants the two-step wizard to use `.wsp2` as its session file extension. No backward compatibility with `.wsprof` is desired in the GUI.

## Files to modify

### 1. `wsprofiler/ui/two_step_dialog.py`
- **Line 1054**: Change default save filename from `.wsprof` to `.wsp2`.
  - `default_name = f"{Path(target_text).name}.wsp2" if target_text else "session.wsp2"`
- **Line 1060**: Change save dialog filter.
  - `"wsprofiler sessions (*.wsp2)"`
- **Line 1078**: Change open/resume dialog filter (no backward compatibility).
  - `"wsprofiler sessions (*.wsp2);;All files (*)"`
- **Line 1021**: Update docstring for `resume()` classmethod.

### 2. `wsprofiler/session.py`
- **Line 1**: Update module docstring.
- **Line 90**: Update `save_session()` docstring.
- **Line 95**: Update parameter docstring.
- **Line 134**: Update `load_session()` docstring.
- **Line 139**: Update parameter docstring.

### 3. `wsprofiler/ui/session_controller.py`
- **Line 21**: Update `SessionController.save()` docstring.
- **Line 65**: Update `SessionController.resume()` docstring.
- **Line 70**: Update parameter docstring.
- **Line 96**: Update `_auto_increment_path()` docstring example.

### 4. `wsprofiler/tests/test_session.py`
- **Line 50**: Rename test file from `"session.wsprof"` to `"session.wsp2"`.
- **Line 74**: Rename test file from `"bad.wsprof"` to `"bad.wsp2"`.
- **Line 109**: Rename test file from `"session.wsprof"` to `"session.wsp2"`.

### 5. `wsprofiler/tests/test_session_integration.py`
- **Line 147**: Rename test file from `"testtarget.wsprof"` to `"testtarget.wsp2"`.
- **Line 164**: Rename test file from `"session.wsprof"` to `"session.wsp2"`.
- **Lines 168, 172**: Update assertion expectations from `_1.wsprof` / `_2.wsprof` to `_1.wsp2` / `_2.wsp2`.

### 6. `.slim/deepwork/save-and-progress.md`
- **Lines 39, 70, 96, 140**: Update all references from `.wsprof` to `.wsp2`.

## Verification
1. Run the test suite: `pytest wsprofiler/tests/test_session.py wsprofiler/tests/test_session_integration.py -v`
2. Ensure all tests pass.
3. Optionally, run a quick grep to confirm no `.wsprof` references remain in source code (excluding `.git`, `.venv`, `dist`, `build`):
   `grep -r "\.wsprof" wsprofiler/ --include="*.py"`
4. Launch the application and verify:
   - Clicking **Save Session** presents a dialog defaulting to `.wsp2` and filtering for `.wsp2`.
   - Clicking **Load Session** (on the Generate page before chart generation) presents a dialog filtering for `.wsp2`.

## Notes
- The `_auto_increment_path()` helper in `session_controller.py` is suffix-agnostic; only its docstring example needs updating.
- The core `save_session()` / `load_session()` functions do not enforce extensions programmatically, so no logic changes are needed there beyond docstrings.
